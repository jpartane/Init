name: 10.60.1.1

# Use the output of the previous command to find the name and IP address of the
# device that makes the link between you (local network) and the outside world
# 'nat' stands for Network Address Translation. nat is the device that 
# makes the link between local network and outside world
# traceroute slash16.org | grep "nat"
