service ssh status

# command that determines the state of the SSH service
# First thing needed is to install a SSH server with command:
# apt-get install openssh-server
# I need to be in root, in order to do so: su
# SSH (Secure Shell) is a method for secure remote login from one computer
# to another with a strong encryption
